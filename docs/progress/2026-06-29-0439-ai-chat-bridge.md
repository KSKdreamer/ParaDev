# AI Chat Bridge Contract

Time: 2026-06-29 04:39 CST

Continued the AI feature loop by adding the first SDK-owned non-streaming desktop chat bridge. This does not mount the floating chat UI yet; it establishes the Python, REST, Tauri, and TypeScript contract that the floating shell should call.

- Added `desktop_chat(...)` and `AI_CHAT_SCHEMA` to the public desktop facade, using HeavenBase `LLM(...).chat(...)` with DeepSeek/OpenAI-compatible route defaults in the frontend contract.
- Added REST `POST /desktop/ai/chat`, Tauri `paradev_chat`, and TypeScript `chatWithParaDevAi(...)` with Tauri-first/native-web fallback.
- Added frontend operation `ai.chat`, `ai` group, `ai-chat` workspace section, REST planner body routing, generated TypeScript contract updates, and regenerated user-manual API references.
- Updated architecture parity docs so GUI AI chat is explicitly mapped to `desktop_chat(...)` and the REST route.

Subagent notes:

- API alignment audit recommended keeping this first slice to one non-streaming prompt, no history, no tool calls, and no GUI state.
- GUI audit recommended mounting the future floating shell in `AppShell` after `main`, below boot progress, with a bottom-right fixed launcher and no topbar button.

Validation:

- `rtk bash scripts/test.bash tests/test_desktop_api_selection.py -q -k 'desktop_ai_chat or desktop_config_rest_routes_forward_dependency_and_llm_requests'`: 2 passed.
- `rtk npm --prefix apps/desktop run test:unit -- src/services/paradev.test.ts`: 19 passed.
- `rtk cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml -- --check`: passed.
- `rtk cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`: 24 passed.
- `rtk npm --prefix apps/desktop run test:unit`: 44 files passed, 579 tests passed.
- `rtk npm --prefix apps/desktop run build`: passed; Vite reported the existing large-chunk warning.
- `rtk uv run python .agents/skills/heaven-style/scripts/scan.py src/paradev/desktop src/paradev/sdk/frontend_api.py src/paradev/surfaces/rest.py tests/test_desktop_api_selection.py tests/test_architecture.py tests/test_cli.py`: passed.
- `rtk bash scripts/flake.bash --ci`: passed.
- `rtk bash scripts/test.bash`: 1159 passed, 1 warning.

PIHC3 status: clean on `v3.1`; no PIHC3 files changed.
