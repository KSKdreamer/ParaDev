# 2026-06-07 18:29 PDX Token Rows

## Scope

- Continued foundation work on `codex/scaffold-source-root-selection`.
- Focused on TAL-291 PDX parser stabilization and shared CLI/SDK inspection payloads.
- Added opt-in lexer token rows for parser, editor, importer, GUI, and MCP debugging without changing the default parse mental model.

## Changes

- Added `Token.to_dict()` with JSON-safe `type`, `value`, `line`, and `column` fields.
- Added `paradev parse --tokens --json` to include token rows in the `paradev.pdx.parse.v1` payload when parsing succeeds.
- Kept token rows opt-in; normal `paradev parse --json` output remains unchanged.
- Changed the parse command to read the source file once and reuse the same source text for parse, dump, and token projection.
- Updated English and Chinese user manual sections in `getting-started` and `troubleshooting`.

## Verification

- Red tests first:
  - `rtk uv run pytest tests/test_pdx_token.py::test_token_to_dict_returns_json_safe_source_span tests/test_cli.py::test_parse_can_include_token_rows_json` failed with missing `Token.to_dict()` and missing `--tokens`.
- Focused green:
  - `rtk uv run pytest tests/test_pdx_token.py::test_token_to_dict_returns_json_safe_source_span tests/test_cli.py::test_parse_can_include_token_rows_json`.
  - `rtk uv run pytest tests/test_pdx_token.py tests/test_cli.py -k 'token_to_dict or parse'`.
- CLI smoke:
  - `rtk uv run paradev parse demos/assets/projects/minimal/src/modules/focus/GER_sample/def.pdx --tokens --json`.
- Format:
  - `rtk uv run black src/paradev/pdx/token.py src/paradev/cli.py tests/test_pdx_token.py tests/test_cli.py`.
- Heaven-style scan:
  - `rtk uv run python .claude/skills/heaven-style/scripts/scan.py src/paradev/pdx/token.py src/paradev/cli.py tests/test_pdx_token.py tests/test_cli.py`.
- Whitespace review:
  - `rtk git diff --check`.
- Lint gate:
  - `rtk bash scripts/flake.bash --ci`.
- Test gate:
  - `rtk bash scripts/test.bash` (`347 passed`).
- Build gate:
  - `rtk uv build`.

## Review

- `Token.to_dict()` is the shared SDK/CLI row contract; the CLI does not invent a separate token shape.
- EOF is omitted from parse payload token rows because users and integrations need source tokens, not stream sentinels.
- `--tokens` stays optional so ordinary mod authors still see the lossy parse projection by default.
- No PIHC3 migration files were touched.

## Linear

- Attempted to read `TAL-291`; Linear MCP returned `UNAUTHORIZED` / `Session expired`.
- Attempted to post a progress comment to `TAL-291`; Linear MCP returned the same auth error.
