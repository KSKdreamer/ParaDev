# Locale Parity

## Slice

- Added a desktop locale parity test so every English UI key must have a Chinese entry.
- Filled the missing Chinese module descriptions, module tab subtitles, and module browser/editor labels.
- Kept the change to display copy only; no SDK, bridge, or PIHC3 source behavior changed.

## Native PIHC3 Smoke

- Ran `rtk bash scripts/run.bash --tauri` against the real PIHC3 project.
- Verified the app loaded in Chinese with PIHC3 data and opened the Event module editor.
- Confirmed translated module/editor controls rendered without obvious clipping or overlap.

Screenshot kept outside the repo:

- `/tmp/paradev-i18n-module.png`

## Verification

- Red first:
  - `rtk npm --prefix apps/desktop test -- src/i18n/locales.test.ts`
- Green checks:
  - `rtk npm --prefix apps/desktop test -- src/i18n/locales.test.ts`
  - `rtk npm --prefix apps/desktop test -- src/i18n/locales.test.ts src/aiChatProfileText.test.ts src/data/shell.test.ts src/components/Workspace.test.tsx src/moduleEditor/ModuleEntityDetails.test.tsx`
  - `rtk npm --prefix apps/desktop run build`
  - `rtk npm --prefix apps/desktop test`
  - `rtk bash scripts/flake.bash --ci`
  - `rtk bash scripts/test.bash`

## Notes

- The production desktop build still emits the existing large-chunk warning for editor bundles.
- Read-only GUI/API scout queued next small slices: derive shell navigation from SDK/frontend API workspace sections, route Tauri app-config commands through the Python desktop facade, decide `paradev.project.name` GUI policy, localize startup progress aria, and remove English status strings from the build-page model.
