# Source Slots Progress

Date: 2026-06-06 18:52 CST

Linear: TAL-294

## Done

- Added generic `Slot` declarations and `match_slots(...)`.
- Supported exact, glob, and regex matching with deterministic POSIX-relative paths.
- Supported `many=True` for deterministic multi-source registration.
- Added diagnostics for missing required slots, too many matches, and source ownership collisions.
- Kept the matcher game-neutral so future families can declare slots without central routing.

## Verification

- `rtk uv run pytest tests/test_build_slots.py -q`
- `rtk bash scripts/test.bash`
- `rtk bash scripts/flake.bash --ci`
- `rtk uv run python .claude/skills/heaven-style/scripts/scan.py src/paradev/build tests/test_build_slots.py`

## Risks Or Blockers

- Live Linear sync is still pending because no Linear MCP tool or `linear` CLI is available in this session.
- Existing local desktop and README edits remain outside this source-slot slice.

## Next

- Add metadata and PDX source loaders that convert matched slots into module records for the build planner.
