# Build Records Progress

Date: 2026-06-06 18:43 CST

Linear: TAL-293

## Done

- Added the first generic build records: `Module`, `Collection`, `Artifact`, `Diagnostic`, and `BuildResult`.
- Added dry-run `BuildResult.plan(...)` for JSON-safe build summaries without filesystem output.
- Added generic artifact path collision diagnostics with stable code `build.artifact_path_collision`.
- Kept this slice game-neutral; no HoI4 family behavior or artifact writers were introduced.

## Verification

- `rtk uv run pytest tests/test_build_records.py -q`
- `rtk bash scripts/test.bash`
- `rtk bash scripts/flake.bash --ci`
- `rtk uv run python .claude/skills/heaven-style/scripts/scan.py src/paradev/build tests/test_build_records.py`

## Risks Or Blockers

- `src/paradev/build/` is covered by the repo's broad `build/` ignore rule, so intentional source files in that package must be force-added until the ignore policy is narrowed.
- Live Linear sync is still pending because no Linear MCP tool or `linear` CLI is available in this session.

## Next

- Add registry and manifest helpers so a test family can emit planned artifacts into a dry-run `BuildResult`.
