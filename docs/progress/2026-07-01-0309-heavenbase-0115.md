# 2026-07-01 03:09 - HeavenBase 0.1.1.5 compatibility

## Slice

Moved ParaDev onto `heavenbase==0.1.1.5` and synchronized the bundled `heaven-style` skill from Blueprint 0.1.1.5. The catalog adapter now uses `HeavenBase.temp(...)` for scratch workspaces and keeps fresh bulk upserts on public chunked `upsert_many(..., overwrite=False)` calls instead of building row ops against removed internals.

Also fixed the native Tauri AI profile bridge so real project roots such as `/Users/magolor/Utils/ParaDev-3/projects/PIHC3` are accepted. Profile ids, config keys, dependency ids, and env var names still use the stricter helper.

## Red

- `rtk uv run --with heavenbase==0.1.1.5 pytest tests/test_hb.py tests/test_project.py tests/test_project_build.py tests/test_desktop_api_selection.py tests/test_cli.py -q` initially failed because `HeavenBase.__init__` no longer accepts `isolated` and `_SchemaService.materialize` moved behind writer/materializer internals.
- `rtk cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml project_root_helper_arg_accepts_normal_filesystem_paths` initially failed to compile because the path-specific helper did not exist.

## Verification

- `rtk uv run --with heavenbase==0.1.1.5 pytest tests/test_hb.py tests/test_project.py tests/test_project_build.py tests/test_desktop_api_selection.py tests/test_cli.py -q`
- `rtk bash scripts/sync-env.bash --check`
- `rtk uv run python .agents/skills/heaven-style/scripts/scan.py src/paradev/hb/__init__.py`
- `rtk uv run pytest tests/test_hb.py::test_hb_catalog_smoke_registers_preview_rows_without_writing tests/test_hb.py::test_hb_catalog_write_bounds_catalog_fields_without_dropping_target_data -q`
- `rtk uv run pytest tests/test_hb.py tests/test_project.py tests/test_project_build.py tests/test_desktop_api_selection.py tests/test_cli.py -q`
- `rtk bash scripts/flake.bash --ci`
- `rtk bash scripts/test.bash`
- `rtk cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml -- --check`
- `rtk cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`
- `rtk env PARADEV_PROJECTS=projects/PIHC3 bash scripts/run.bash --tauri --port 47840`

## Notes

- Normal `rtk uv run python -c "import heavenbase; print(heavenbase.__version__)"` now reports `0.1.1.5`.
- Selected HeavenBase-facing compatibility suite passed with `512 passed, 224 warnings`.
- Fast repo gate passed with `1235 passed, 289 warnings`.
- Tauri Rust tests passed with `23 passed`.
- PIHC3 Tauri smoke reached `Running target/debug/paradev-desktop`; after the bridge fix, the previous `Failed to load ParaDev AI chat profiles. project root contains unsupported characters.` warning did not reappear during the smoke window.
