# PIHC3 Projectwide Bytecode Clean Progress

Date: 2026-06-21 11:01

Linear: TAL-297

## Done

- Tightened the PIHC3 `compile.bash --clean` and `--clean-only` cleanup path to remove Python bytecode caches anywhere under the PIHC3 project tree while pruning the nested `.git` directory.
- Simplified PIHC3 ignore/docs wording so generated bytecode is described as projectwide local-only state, not only `scripts/` or `system/` state.
- Extended the migration contract test to seed generated bytecode under `system/`, `scripts/`, `scripts/review/`, and `src/modules/`.

## Verification

- Red: `rtk uv run pytest tests/test_pihc3_migration_contracts.py -q -k "build_wrappers_use_current_paradev_surfaces or compile_clean_only_removes_generated_outputs_without_build or generated_runtime_directories_are_local_only"` failed before the wrapper cleanup was broadened.
- Green: `rtk uv run pytest tests/test_pihc3_migration_contracts.py -q -k "build_wrappers_use_current_paradev_surfaces or compile_clean_only_removes_generated_outputs_without_build or generated_runtime_directories_are_local_only or project_tree_has_no_generated_cache_files"`
- `rtk bash -n projects/PIHC3/compile.bash`
- `rtk uv run python .agents/skills/heaven-style/scripts/scan.py tests/test_pihc3_migration_contracts.py`
- `rtk git diff --check -- projects/PIHC3/compile.bash projects/PIHC3/README.md projects/PIHC3/.gitignore docs/user-manual/pihc3.md tests/test_pihc3_migration_contracts.py`
- `rtk bash --noprofile --norc -c 'find projects/PIHC3 \( -path projects/PIHC3/.git -prune \) -o \( -name "__pycache__" -o -name "*.pyc" -o -name ".DS_Store" \) -print | sort | sed -n "1,120p"'`
- `rtk bash projects/PIHC3/compile.bash --clean --summary --json`

## Risks Or Blockers

- The compact PIHC3 summary still loads the full migrated project and takes over a minute; the output is compact, but it is not yet a cheap metadata-only health check.

## Next

- Keep pruning PIHC3 generated/local-only residue as importer and GUI smoke runs create it, and continue moving normal authoring paths through the single `compile.bash` wrapper.
