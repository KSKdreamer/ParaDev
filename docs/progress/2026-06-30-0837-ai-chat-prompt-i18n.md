# AI Chat Prompt I18n Progress

Date: 2026-06-30 08:37

Linear: active goal

## Done

- Added SDK-owned `promptKey` metadata to built-in desktop AI chat profiles and regenerated the desktop TypeScript contract.
- Localized the built-in AI chat profile prompts in English and Chinese while preserving raw English prompts for the Python-side LLM role prompt.
- Extended the desktop profile text helper so prompt display/save behavior matches label/detail behavior: translated defaults are shown in the UI but not saved back as overrides.
- Added a shared desktop source-kind text helper so the floating chat and config profile editor show localized unknown source labels instead of raw internal ids.

## Verification

- Red first: `rtk bash scripts/test.bash --serial tests/test_desktop_api_selection.py::test_desktop_typescript_contract_exports_ai_chat_profile_catalog -q` failed on the missing generated `promptKey`.
- Red first: `rtk npm --prefix apps/desktop run test:unit -- src/services/paradev.test.ts -t "loads AI chat profiles through the Tauri desktop bridge"` failed because `promptKey` was not normalized.
- Red first: `rtk npm --prefix apps/desktop run test:unit -- src/aiChatProfileText.test.ts -t "does not save unchanged translated built-in prompts"` failed because translated prompts were treated as custom overrides.
- Red first: `rtk npm --prefix apps/desktop run test:unit -- src/configPage/ConfigPage.test.tsx -t "renders built-in AI chat profile prompts through the selected locale|renders AI chat source toggles from the SDK profile catalog"` failed on raw English prompt text and raw unknown source ids.
- Red first: `rtk npm --prefix apps/desktop run test:unit -- src/components/FloatingChatShell.test.tsx -t "uses localized unknown source labels instead of raw internal source kinds"` failed on raw unknown source ids.
- `rtk npm --prefix apps/desktop run test:unit -- src/i18n/locales.test.ts -t "keeps AI chat prompt and unknown source fallback translations localized"`
- `rtk bash scripts/test.bash --serial tests/test_desktop_api_selection.py -q -k "desktop_typescript_contract or desktop_chat_profile or ai_chat_profile"`
- `rtk npm --prefix apps/desktop run test:unit -- src/services/paradev.test.ts src/aiChatProfileText.test.ts src/configPage/ConfigPage.test.tsx src/components/FloatingChatShell.test.tsx src/i18n/locales.test.ts`
- `rtk bash scripts/test.bash --serial tests/test_cli.py::test_desktop_api_cli_outputs_typescript_config_keys -q`
- `rtk uv run python .agents/skills/heaven-style/scripts/scan.py src/paradev/desktop/local.py tests/test_desktop_api_selection.py`
- `rtk npm --prefix apps/desktop run test:unit`
- `rtk npm --prefix apps/desktop run build`
- `rtk git diff --check`
- `rtk bash scripts/flake.bash --ci`
- `rtk bash scripts/test.bash`
- `rtk env PARADEV_PROJECTS=projects/PIHC3 bash scripts/run.bash --tauri`

## Risks Or Blockers

- The desktop build still reports the existing large chunk warning.
- The standard fast test gate reported two existing warnings.

## Next

- Add interaction-level tests for editing/saving AI profile prompts and for floating chat keyboard/send behavior.
