# 2026-06-15 PDX Core API Reference

Added a generated `paradev.pdx` facade API table and `pdx-core-api` CLI command so tokenizer, token, AST, parser, diagnostics, scalar constants, and PDX reference helper exports are auditable without copying `__all__` by hand.

Updated the aggregate API catalog, CLI command table, surfaces facade reference, manual index, SDK/developer guidance, and architecture boundary map. Focused verification covers the new PDX core table, catalog row/index drift, CLI contract rows, and the new CLI command.
