# Module Build Explain Progress

Date: 2026-06-07 06:02 CST

Linear: unavailable; no Linear connector was exposed by tool discovery and `linear` is not on PATH.

## Done

- Added `paradev.build.explain.v1`, a dry-build explanation payload for one module.
- Exposed `Project.build_explain(module_id=...)` and `paradev build-explain --module ... --json`.
- Included module target metadata, summary counts, related sources, artifacts, outgoing dependency edges, diagnostics, and the related build graph subset.
- Documented the workflow as the user-facing "what happened to this module?" surface.

## Verification

- Red check: `rtk bash scripts/test.bash tests/test_project.py::test_project_build_explain_returns_module_context_without_writing tests/test_project.py::test_project_cli_build_explain_outputs_json -q` failed because `Project.build_explain(...)` and `build-explain` did not exist.
- Focused green: `rtk bash scripts/test.bash tests/test_project.py::test_project_build_explain_returns_module_context_without_writing tests/test_project.py::test_project_cli_build_explain_outputs_json -q`
- CLI smoke: `rtk uv run paradev build-explain demos/assets/projects/minimal --module focus/GER_sample --json`
- Related: `rtk bash scripts/test.bash tests/test_project.py tests/test_build_manifest.py tests/test_build_records.py tests/test_cli.py tests/test_architecture.py -q`
- Full suite: `rtk bash scripts/test.bash`
- Lint: `rtk bash scripts/flake.bash --ci`
- Heaven scan: `rtk uv run python .claude/skills/heaven-style/scripts/scan.py src/paradev/build/explain.py src/paradev/build/__init__.py src/paradev/sdk/project.py src/paradev/cli.py tests/test_project.py`
- Diff hygiene: `rtk git diff --check`

## Review

- The explain payload composes existing manifest and graph builders instead of creating a second traceability model.
- The CLI keeps the beginner-facing API focused on a module id, while lower-level filtering remains in `source-map`, `dependencies`, and `build-graph`.
- Unrelated desktop and README work remains outside this slice and should stay unstaged.

## Risks Or Blockers

- The payload currently reports outgoing module dependencies; incoming reference explainability can be added when editor or MCP flows need it.
- Linear status could not be updated from this environment.

## Next

- Add first generic compiler diagnostics that use explain payloads to point users from an artifact or dependency back to authored slots.
- Continue toward typed graph queries only after GUI/MCP usage clarifies the traversal questions.
