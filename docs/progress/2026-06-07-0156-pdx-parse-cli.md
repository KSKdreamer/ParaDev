# 2026-06-07 01:56 CST - PDX Parse CLI

## Done

- Added `paradev parse <path> --json` as the first direct CLI surface over the PDX parser.
- Returned `paradev.pdx.parse.v1` payloads with resolved path, file extension, `ok`, data projection, and structured diagnostics.
- Kept invalid PDX script-friendly: the command prints the diagnostic payload and exits with status 1.
- Documented the parse inspection command in the build-flow guide.

## Verification

- Red first: `rtk bash scripts/test.bash tests/test_cli.py::test_parse_outputs_pdx_projection_json tests/test_cli.py::test_parse_outputs_diagnostics_json_for_invalid_pdx -q` failed because the CLI had no `parse` command.
- Focused green: `rtk bash scripts/test.bash tests/test_cli.py::test_parse_outputs_pdx_projection_json tests/test_cli.py::test_parse_outputs_diagnostics_json_for_invalid_pdx -q`
- Related suite: `rtk bash scripts/test.bash tests/test_cli.py tests/test_pdx_token.py tests/test_pdx_roundtrip.py tests/test_build_loaders.py -q` passed 31 tests.
- Full suite: `rtk bash scripts/test.bash` passed 155 tests.
- Lint: `rtk bash scripts/flake.bash --ci`
- Heaven-style scan: `rtk uv run python .claude/skills/heaven-style/scripts/scan.py src/paradev/cli.py tests/test_cli.py`
- CLI smoke: `rtk uv run paradev parse demos/assets/projects/minimal/src/modules/focus/GER_sample/def.pdx --json`
- Whitespace: `rtk git diff --check -- src/paradev/cli.py tests/test_cli.py docs/workflows/build-flow.md docs/progress/2026-06-07-0156-pdx-parse-cli.md`

## Linear

- `rtk command -v linear` exits with status 1.
- Tool discovery for Linear issue management exposed Codex automation and GitHub tools only; no Linear connector is available in this session.

## Risks

- The parse payload intentionally exposes the lossy `PDXBlock.to_dict()` projection, not the full lossless AST dump. If LSP or MCP callers need lossless payloads, add an explicit flag instead of changing the default shape.
- Existing unrelated desktop, README, and script changes remain intentionally untouched.

## Next

- Add a formatter/check CLI surface only when a user-facing workflow needs it.
- Keep parser payloads aligned between CLI, future MCP tools, and LSP diagnostics.
