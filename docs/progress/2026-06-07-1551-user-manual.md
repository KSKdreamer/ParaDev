# User Manual Progress

Date: 2026-06-07 15:51 +0800

Linear: TAL-295

## Done

- Expanded `docs/user-manual/README.md` into a bilingual manual index for HoI4 modders.
- Added bilingual user-facing pages for getting started, project layout, modules and collections, build diagnostics, Python SDK usage, PIHC3 status, and troubleshooting.
- Added a bilingual developer manual covering SDK boundaries, project-local families, Python registry modules, and repo gates.
- Documented the new `paradev new` and `Project.create(...)` starter workflow from the user's perspective.

## Verification

- TAL-295 read attempt failed because Linear returned `UNAUTHORIZED; Session expired. Please re-authenticate.`
- `rtk rg -n "paradev|Project.load|PIHC3|diagnostics|build" docs/user-manual README.en.md docs/workflows/build-flow.md` passed.
- `rtk uv run paradev summary demos/assets/projects/minimal --json` passed.
- `rtk uv run python - <<'PY' ... Project.load("demos/assets/projects/minimal").summary() ... PY` passed.
- `rtk zsh -lc 'tmp=$(mktemp -d); uv run paradev new "$tmp/starter-mod" --title "Starter Mod" --json >/tmp/paradev-new.json; uv run paradev build "$tmp/starter-mod" --emit-artifacts --emit-manifests --json >/tmp/paradev-build.json; test -f "$tmp/starter-mod/build/mod/common/modifiers/starter_mod_starter_modifier.txt"'` passed.
- `rtk git diff --check -- docs/user-manual` passed.
- Manual local-link check passed.

## Linear Sync

- TAL-295 could not be read or updated because the Linear MCP session is expired.

## Risks Or Blockers

- The manual intentionally marks PIHC3 bootstrap, descriptor `.mod` generation, image conversion, and full HoI4 compilers as alpha or not ready.
- README integration remains separate because `README.en.md` already has unrelated unstaged edits in this worktree.

## Next

- Keep the manual synchronized as descriptor/launcher artifacts, PIHC3 bootstrap, and additional HoI4 families land.
