# PDX Core Progress

Date: 2026-06-06 18:32 CST

Linear: TAL-290, TAL-291

## Done

- Continued the HeavenBase-aligned config CLI slice by verifying nested `config`/`cfg` commands can emit JSON and read `paradev.project.name`.
- Implemented the first PDX core slice: tokenizer exports, structured diagnostics, `PDXScalar`, `PDXEntry`, `PDXBlock`, parser, formatter, lossless dump/load, lossy `to_dict`, duplicate-key projection, comments, comparison operators, color scalars, and unclosed-block diagnostics.
- Preserved the minimal user model: `from paradev.pdx import PDXBlock` remains the public entry point.

## Verification

- `rtk uv run pytest tests/test_pdx_token.py tests/test_pdx_roundtrip.py -q`
- `rtk uv run pytest tests/test_cli.py -q`
- `rtk bash scripts/test.bash`
- `rtk uv run python .claude/skills/heaven-style/scripts/scan.py src/paradev/pdx src/paradev/cli.py tests/test_cli.py tests/test_pdx_token.py tests/test_pdx_roundtrip.py`
- `rtk bash scripts/flake.bash --ci`
- `rtk uv run paradev config list --json`
- `rtk uv run paradev cfg get paradev.project.name`
- `rtk uv run python - <<'PY' ... PDXBlock.from_str(...) ... PY`

## Risks Or Blockers

- Live Linear sync is pending because this session has no Linear MCP tool and no `linear` CLI on PATH. The local issue map remains in `docs/plans/linear.md`.
- The worktree contains unrelated desktop and README edits that were not part of this foundation slice.

## Next

- Add representative `.txt`, `.gfx`, `.gui`, and `.asset` PDX samples and broaden parser round-trip coverage.
- Start TAL-292 project manifest discovery once TAL-291 has sample coverage and any live Linear status can be synced.
