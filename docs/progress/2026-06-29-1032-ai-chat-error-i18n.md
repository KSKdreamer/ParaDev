# AI Chat Error I18n

## Slice

- Localized the desktop AI chat fallback error for SDK/bridge responses that return `status: "error"` without `detail` or `reply`.
- Added a focused regression around the fallback so Chinese UI does not render the hardcoded English route error.
- Fixed a parallel test race where tests that mutate shared `CM_PARADEV` values could restore `paradev.cli.output` while another xdist worker was asserting JSON-by-default behavior.

## Native PIHC3 Smoke

- Ran `rtk bash scripts/run.bash --tauri` against the real PIHC3 project.
- Verified the Chinese desktop shell loaded PIHC3 module counts and the floating chat launcher.
- Opened the chat panel through macOS accessibility and verified localized labels: `AI 聊天`, `任务`, `暂无消息`, `询问 ParaDev`, and `发送`.

Screenshots kept outside the repo:

- `/tmp/paradev-tauri-ai-chat-fallback-launcher.png`
- `/tmp/paradev-tauri-ai-chat-fallback-open-panel.png`

## Verification

- `rtk npm --prefix apps/desktop run test:unit -- --run src/App.test.ts` failed first because `aiChatReplyText` did not exist.
- `rtk bash scripts/test.bash --parallel tests/test_cli.py::test_cli_output_config_enables_json_by_default tests/test_desktop_api_selection.py::test_desktop_config_value_round_trips_cli_output -q` reproduced the `CM_PARADEV` race before the lock fixture and passed after it.
- `rtk uv run python .agents/skills/heaven-style/scripts/scan.py tests/conftest.py tests/test_cli.py tests/test_desktop_api_selection.py tests/test_native_web_bridge.py`
- `rtk npm --prefix apps/desktop run test:unit -- --run src/App.test.ts src/components/FloatingChatShell.test.tsx src/services/paradev.test.ts`
- `rtk npm --prefix apps/desktop run test:unit`
- `rtk npm --prefix apps/desktop run build`
- `rtk bash scripts/flake.bash --ci`
- `rtk bash scripts/test.bash`
- `rtk git diff --check`
- `rtk git -C projects/PIHC3 status --short --untracked-files=all`

## Follow-Ups

- Make `paradev.ai.key_env` fully drive backend AI route key selection instead of remaining GUI-visible only.
- Localize built-in fallback AI chat profile labels/details from `apps/desktop/src/services/paradev.ts`.
- Consider PIHC3 minimization slices from the read-only audit: byte-identical legacy PNG duplicate removal and idea icon migration out of `legacy/`.
