# PDX Samples Progress

Date: 2026-06-06 18:36 CST

Linear: TAL-291

## Done

- Added representative PDX demo inputs for `.txt`, `.gfx`, `.gui`, and `.asset` files under `demos/assets/pdx/`.
- Extended round-trip tests so each sample parses through `PDXBlock.from_file`, renders through `to_str`, reparses, survives lossless `dump`/`load`, remains JSON-safe through `to_dict`, and preserves file extension metadata for output.

## Verification

- `rtk uv run pytest tests/test_pdx_roundtrip.py::test_representative_pdx_samples_round_trip -q`
- `rtk bash scripts/test.bash`
- `rtk bash scripts/flake.bash --ci`
- `rtk uv run python .claude/skills/heaven-style/scripts/scan.py tests/test_pdx_roundtrip.py`

## Risks Or Blockers

- Live Linear sync is still pending because no Linear MCP tool or `linear` CLI is available in this session.
- Existing local desktop and README edits remain outside this PDX sample slice.

## Next

- Begin TAL-292 by implementing `paradev.yaml` project discovery and a JSON-safe project view.
